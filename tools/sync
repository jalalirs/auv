#!/usr/bin/env bash
# Compatibility entry point. Git is the only supported source-sync mechanism.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/gpu" sync "$@"
