# Architecture decisions

Record decisions that constrain future work as numbered Markdown files. Each
record states its context, decision, consequences, and status. Decisions can be
superseded, but history should not be rewritten.
