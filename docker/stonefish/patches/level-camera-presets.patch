diff --git a/Library/include/graphics/OpenGLTrackball.h b/Library/include/graphics/OpenGLTrackball.h
--- a/Library/include/graphics/OpenGLTrackball.h
+++ b/Library/include/graphics/OpenGLTrackball.h
@@ -73,7 +73,8 @@ namespace sf
          */
         void GlueToMoving(MovingEntity* ent);
-        //! A method to follow the full pose of a moving body from a preset view.
+        //! A method to follow a moving body from a preset view.
         void FollowMoving(MovingEntity* ent, glm::vec3 localDirection,
                           glm::vec3 localUp, GLfloat orbitRadius,
+                          bool keepLevel = false,
                           glm::vec3 localCenterOffset = glm::vec3(0.f));
         //! A method saving the new centre for update.
@@ -138,5 +139,6 @@ namespace sf
         MovingEntity* holdingEntity;
         bool followOrientation;
+        bool keepLevel;
         glm::vec3 localCenterOffset;
         glm::vec3 localDirection;
         glm::vec3 localUp;
diff --git a/Library/src/graphics/OpenGLTrackball.cpp b/Library/src/graphics/OpenGLTrackball.cpp
--- a/Library/src/graphics/OpenGLTrackball.cpp
+++ b/Library/src/graphics/OpenGLTrackball.cpp
@@ -52,6 +52,7 @@ OpenGLTrackball::OpenGLTrackball(glm::vec3 centerPosition, GLfloat orbitRadius,
     continuous = true;
     holdingEntity = nullptr;
     followOrientation = false;
+    keepLevel = false;
     localCenterOffset = glm::vec3(0.f);
     localDirection = glm::vec3(0.f, -1.f, 0.f);
     localUp = glm::vec3(0.f, 0.f, 1.f);
@@ -121,12 +122,19 @@ void OpenGLTrackball::UpdateCenterPos()
         tempCenter = glm::vec3((GLfloat)org.x(), (GLfloat)org.y(), (GLfloat)org.z());
         if(followOrientation)
         {
-            Vector3 dir = transform.getBasis() * Vector3(localDirection.x,
-                                                         localDirection.y,
-                                                         localDirection.z);
-            Vector3 up = transform.getBasis() * Vector3(localUp.x,
-                                                        localUp.y,
-                                                        localUp.z);
+            Matrix3 cameraBasis = transform.getBasis();
+            if(keepLevel)
+            {
+                Vector3 forward = cameraBasis * Vector3(1, 0, 0);
+                Scalar heading = atan2(forward.y(), forward.x());
+                cameraBasis = Matrix3(Quaternion(heading, 0, 0));
+            }
+            Vector3 dir = cameraBasis * Vector3(localDirection.x,
+                                                localDirection.y,
+                                                localDirection.z);
+            Vector3 up = cameraBasis * Vector3(localUp.x,
+                                               localUp.y,
+                                               localUp.z);
             tempDirection = glm::normalize(glm::vec3((GLfloat)dir.x(),
                                                      (GLfloat)dir.y(),
                                                      (GLfloat)dir.z()));
@@ -238,5 +246,6 @@ void OpenGLTrackball::GlueToMoving(MovingEntity* ent)
         rotation = glm::inverse(glm::quat_cast(cameraToWorld));
     }
     followOrientation = false;
+    keepLevel = false;
     localCenterOffset = glm::vec3(0.f);
     holdingEntity = ent;
@@ -251,10 +260,12 @@ void OpenGLTrackball::FollowMoving(MovingEntity* ent,
                                    glm::vec3 localDirection_,
                                    glm::vec3 localUp_,
                                    GLfloat orbitRadius,
+                                   bool keepLevel_,
                                    glm::vec3 localCenterOffset_)
 {
     holdingEntity = ent;
     followOrientation = ent != nullptr;
+    keepLevel = keepLevel_;
     localDirection = glm::normalize(localDirection_);
     localUp = glm::normalize(localUp_);
     localCenterOffset = localCenterOffset_;
diff --git a/Library/src/core/GraphicalSimulationApp.cpp b/Library/src/core/GraphicalSimulationApp.cpp
--- a/Library/src/core/GraphicalSimulationApp.cpp
+++ b/Library/src/core/GraphicalSimulationApp.cpp
@@ -471,22 +471,23 @@ void GraphicalSimulationApp::SetCameraPreset(unsigned int preset)
         case 1: // Chase from behind and slightly above.
             trackball->FollowMoving(trackballCenter,
                                     glm::vec3(1.f, 0.f, 0.22f),
-                                    glm::vec3(0.f, 0.f, -1.f), 4.5f);
+                                    glm::vec3(0.f, 0.f, -1.f), 4.5f, true);
             break;
         case 2: // Top-down, with the vehicle nose pointing up-screen.
             trackball->FollowMoving(trackballCenter,
                                     glm::vec3(0.f, 0.f, 1.f),
-                                    glm::vec3(1.f, 0.f, 0.f), 7.f);
+                                    glm::vec3(1.f, 0.f, 0.f), 7.f, true);
             break;
         case 3: // Look at the vehicle head-on.
             trackball->FollowMoving(trackballCenter,
                                     glm::vec3(-1.f, 0.f, 0.f),
-                                    glm::vec3(0.f, 0.f, -1.f), 4.f);
+                                    glm::vec3(0.f, 0.f, -1.f), 4.f, true);
             break;
         case 4: // Nose-mounted first-person view.
             trackball->FollowMoving(trackballCenter,
                                     glm::vec3(1.f, 0.f, 0.f),
                                     glm::vec3(0.f, 0.f, -1.f), 0.05f,
+                                    false,
                                     glm::vec3(0.65f, 0.f, 0.f));
             break;
         default:
