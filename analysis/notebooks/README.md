# Notebooks

Notebooks are for exploration and presentation, not hidden production logic.
Reusable parsing, metrics, and plotting code belongs in `auv_analysis` and must
be testable without executing a notebook.
