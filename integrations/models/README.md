# Scientific-model integrations

Adapters for ocean, wave, weather, drift, atmospheric, and biogeochemical
models will live here.

Each adapter prepares a versioned request, executes a finite model job, retains
native results, and emits a canonical environmental package. No model is an
unconditional dependency merely because it appears on the candidate list.
