# Lab 01 — Frames and vehicle description

## Objective

Build and inspect an explicit vehicle frame tree with no ambiguous axes.

## Completion check

Visualize `map`, `odom`, `base_link`, buoyancy, thruster, and sensor frames;
explain every transform; and detect one deliberately incorrect transform.
