# GPU development deployment

Future desired state for the shared GPU box: product services, scientific job
execution, Isaac sessions, development storage, observability, and resource
policy.

The Git checkout is synchronized through `tools/gpu`; large runtime data lives
outside the checkout.
