# Manifests

This directory is reserved for reviewed, versioned metadata pointing to
external surveys, observations, model inputs, and derived artifacts.

No manifest will be added before the shared contract is reviewed.
