# Packages

Shared, versioned contracts and clients live here. Packages must remain small,
purposeful, and independent of deployable applications whenever possible.

- `contracts/` defines language-neutral system boundaries.
- `clients/` contains approved client libraries generated or implemented from
  those contracts.
